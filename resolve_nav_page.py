from pathlib import Path

path = Path('/home/user/workspace/hike/src/app/navigate/[planId]/page.tsx')
s = path.read_text()

def replace(old: str, new: str) -> None:
    global s
    assert old in s, f'Missing expected conflict block:\n{old[:160]}'
    s = s.replace(old, new, 1)

replace('''  normalizeHeading,\n<<<<<<< HEAD\n=======\n  progressAlongTrail,\n  remainingElevationGain,\n  stabilizeLoop,\n>>>>>>> origin/main\n  travelDirectionAlong,''', '''  normalizeHeading,\n  travelDirectionAlong,''')
replace('''import {\n  loadCachedRoutePack,''', '''import {\n  isLikelyOffline,\n  loadCachedRoutePack,''')
replace('''import { createRouteProgressCache, type RouteProgressCache } from "@/lib/offline/progress-cache";''', '''import {\n  createRouteProgressCache,\n  progressWithRouteCache,\n  type RouteProgressCache,\n} from "@/lib/offline/progress-cache";''')
replace('''  const [deniedNeedHeading, setDeniedNeedHeading] = useState(false);\n<<<<<<< HEAD\n  const [navUnlocked, setNavUnlocked] = useState(false);\n  const [wakeHeld, setWakeHeld] = useState(false);\n  const snapHintRef = useRef<{ traveledMeters: number } | null>(null);\n=======\n>>>>>>> 81db907\n  const [deniedError, setDeniedError] = useState<string | null>(null);''', '''  const [deniedNeedHeading, setDeniedNeedHeading] = useState(false);\n  const [navUnlocked, setNavUnlocked] = useState(false);\n  const [wakeHeld, setWakeHeld] = useState(false);\n  const [deniedError, setDeniedError] = useState<string | null>(null);''')
replace('''<<<<<<< HEAD\n  const drFix = useMemo(() => {\n    if (!gpsDenied || !deniedAnchor) return null;\n    const meters =\n      deniedPaces > 0 ? distanceFromPaces(deniedPaces, deniedPaceLen) : 0;\n    const point = deadReckon(deniedAnchor, deniedAnchor.heading, meters);\n    return { ...point, heading: deniedAnchor.heading, meters };\n  }, [gpsDenied, deniedAnchor, deniedPaces, deniedPaceLen]);\n=======\n  useEffect(() => {\n    if (!gpsDenied) return;\n    const refresh = () => setDeniedNow(Date.now());\n    const initial = window.setTimeout(refresh, 0);\n    const id = window.setInterval(refresh, 5000);\n    return () => {\n      window.clearTimeout(initial);\n      window.clearInterval(id);\n    };\n  }, [gpsDenied]);\n\n  const drFix = useMemo(() => {\n    if (!gpsDenied || !deniedAnchor) return null;\n    const pacedMeters =\n      deniedPaces > 0\n        ? distanceFromPaces(deniedPaces, deniedPaceLen)\n        : null;\n    const elapsedMeters =\n      deniedNow >= deniedAnchor.at\n        ? ((deniedNow - deniedAnchor.at) / 1000) * 1.15\n        : null;\n    const meters = pacedMeters ?? elapsedMeters;\n    if (meters == null || !Number.isFinite(meters) || meters <= 0) return null;\n    const point = deadReckon(deniedAnchor, deniedAnchor.heading, meters);\n    return { ...point, heading: deniedAnchor.heading, meters };\n  }, [gpsDenied, deniedAnchor, deniedPaces, deniedPaceLen, deniedNow]);\n>>>>>>> 81db907''', '''  const drFix = useMemo(() => {\n    if (!gpsDenied || !deniedAnchor || deniedPaces <= 0) return null;\n    const meters = distanceFromPaces(deniedPaces, deniedPaceLen);\n    if (!Number.isFinite(meters) || meters <= 0) return null;\n    // Do not advance a denied-GPS fix from elapsed time: a guessed walking speed\n    // would invent a current coordinate that can be mistaken for a live position.\n    const point = deadReckon(deniedAnchor, deniedAnchor.heading, meters);\n    return { ...point, heading: deniedAnchor.heading, meters };\n  }, [gpsDenied, deniedAnchor, deniedPaces, deniedPaceLen]);''')
replace('''<<<<<<< HEAD\n  function enterGpsDenied() {\n    const fix = gps.fix;\n    if (!fix || !gpsTrusted) {\n      setDeniedError("Need a live GPS fix to anchor dead reckoning.");\n      return;\n    }\n    const heading = resolveDeniedHeading();\n    if (heading == null) {\n      setDeniedNeedHeading(true);\n      setDeniedError("No heading yet — enter a bearing or walk until the compass has a heading. Dead reckoning cannot guess which way you are facing.");\n      return;\n    }\n    setDeniedNeedHeading(false);\n    setDeniedError(null);\n    setDeniedAnchor({\n      lat: fix.lat,\n      lng: fix.lng,\n      heading,\n      at: Date.now(),\n    });\n    setDeniedPaces(0);\n    setGpsDenied(true);\n  }\n\n  function exitGpsDenied() {\n    setGpsDenied(false);\n    setDeniedAnchor(null);\n    setDeniedPaces(0);\n    setDeniedNeedHeading(false);\n    setDeniedError(null);\n  }\n=======\n  /**\n   * Dead reckoning needs a real heading. `heading` is null whenever the device is not\n   * moving (and often on iOS Safari), and defaulting it to 0 silently walks the hiker\n   * due north on the map regardless of which way they go.\n   */\n  const toggleDeadReckoning = useCallback(() => {\n    setDeniedError(null);\n    if (gpsDenied) {\n      setGpsDenied(false);\n      setDeniedAnchor(null);\n      setDeniedPaces(0);\n      setDeniedNeedHeading(false);\n      return;\n    }\n    const fix = gps.fix;\n    if (!fix || !gpsTrusted) {\n      setDeniedError("Need a live GPS fix to anchor dead reckoning.");\n      return;\n    }\n    const heading = resolveDeniedHeading();\n    if (heading == null) {\n      setDeniedNeedHeading(true);\n      setDeniedError("No heading yet — walk a few paces in your intended direction, then arm DR. Dead reckoning cannot guess which way you are facing.");\n      return;\n    }\n    const anchoredAt = Date.now();\n    setDeniedNeedHeading(false);\n    setDeniedAnchor({ lat: fix.lat, lng: fix.lng, heading, at: anchoredAt });\n    setDeniedPaces(0);\n    setDeniedNow(anchoredAt);\n    setGpsDenied(true);\n  }, [gpsDenied, gps.fix, gpsTrusted, resolveDeniedHeading]);\n>>>>>>> 81db907''', '''  /**\n   * Dead reckoning needs a real heading. `heading` is null whenever the device is not\n   * moving (and often on iOS Safari), and defaulting it to 0 silently walks the hiker\n   * due north on the map regardless of which way they go.\n   */\n  const toggleDeadReckoning = useCallback(() => {\n    setDeniedError(null);\n    if (gpsDenied) {\n      setGpsDenied(false);\n      setDeniedAnchor(null);\n      setDeniedPaces(0);\n      setDeniedNeedHeading(false);\n      return;\n    }\n    const fix = gps.fix;\n    if (!fix || !gpsTrusted) {\n      setDeniedError("Need a live GPS fix to anchor dead reckoning.");\n      return;\n    }\n    const heading = resolveDeniedHeading();\n    if (heading == null) {\n      setDeniedNeedHeading(true);\n      setDeniedError("No heading yet — walk a few paces in your intended direction, then arm DR. Dead reckoning cannot guess which way you are facing.");\n      return;\n    }\n    setDeniedNeedHeading(false);\n    setDeniedAnchor({ lat: fix.lat, lng: fix.lng, heading, at: Date.now() });\n    setDeniedPaces(0);\n    setGpsDenied(true);\n  }, [gpsDenied, gps.fix, gpsTrusted, resolveDeniedHeading]);''')
replace('''<<<<<<< HEAD\n<<<<<<< HEAD\n    // travelDirection MUST be passed. It was computed and listed in this\n    // effect's dependencies but never forwarded, so "Remaining" silently\n    // counted toward the stored end of the route rather than the end being\n    // walked to -- reintroducing the bug the direction fix was written for,\n    // and with it the silenced turnaround warning.\n=======\n>>>>>>> 81db907\n    const p = progressWithRouteCache(\n      progressCacheRef.current,\n      { lat: navFix.lat, lng: navFix.lng },\n      travelDirection,\n    );\n=======\n    const raw = progressAlongTrail(\n      { lat: navFix.lat, lng: navFix.lng },\n      loadState.pack.geometry,\n      loadState.pack.elevationProfile,\n      snapHintRef.current,\n      travelDirection,\n    );\n    const stabilized = stabilizeLoop(raw, snapHintRef.current);\n    const toEnd = Math.max(stabilized.totalMeters - stabilized.traveledMeters, 0);\n    const toStart = Math.max(stabilized.traveledMeters, 0);\n    const remainingMeters =\n      travelDirection === "backward"\n        ? toStart\n        : travelDirection === "forward"\n          ? toEnd\n          : Math.min(toStart, toEnd);\n    const resolvedDirection =\n      travelDirection !== "unknown" ? travelDirection : toStart <= toEnd ? "backward" : "forward";\n    const p: TrailProgress = {\n      ...stabilized,\n      remainingMeters,\n      remainingDirection: travelDirection,\n      remainingElevationMeters: remainingElevationGain(\n        loadState.pack.elevationProfile,\n        stabilized.traveledMeters,\n        resolvedDirection,\n      ),\n    };\n    if (Number.isFinite(p.traveledMeters)) {\n      snapHintRef.current = { traveledMeters: p.traveledMeters };\n    }\n>>>>>>> origin/main''', '''    // The cached path must receive direction so its remaining-distance calculation\n    // stays identical to progressAlongTrail and cannot reverse the turnaround warning.\n    const p = progressWithRouteCache(\n      progressCacheRef.current,\n      { lat: navFix.lat, lng: navFix.lng },\n      travelDirection,\n    );''')
replace('''  const deniedWarning = gpsDenied\n<<<<<<< HEAD\n    ? `GPS DENIED — dead reckon ${drFix ? `${Math.round(drFix.meters)} m` : "0 m"} on ${deniedAnchor ? `${Math.round(deniedAnchor.heading)}°` : "—"}${drErrorM != null ? `, estimated ±${drErrorM} m and growing` : ""}. SOS / SMS use this DR position.`\n=======\n    ? !drFix\n      ? "GPS DENIED — dead-reckoned position unavailable until the estimate advances. Do not use this screen as a current location."\n      : `GPS DENIED — dead reckon ${Math.round(drFix.meters)} m on ${Math.round(drFix.heading)}°${drErrorM != null ? `, estimated ±${drErrorM} m and growing` : ""}. SOS still uses a live GPS fix if one exists.`\n>>>>>>> 81db907\n    : deniedNeedHeading\n      ? "Need a compass heading or typed bearing before GPS-denied dead reckon."\n      : !gpsTrusted && gps.fix\n        ? `GPS untrusted — last known ${formatFixAge(gps.fix.recordedAt)}. Display only; not used for off-trail or SOS as live.`\n<<<<<<< HEAD\n        : deniedError;\n=======\n        : null;\n>>>>>>> 81db907''', '''  const deniedWarning = gpsDenied\n    ? !drFix\n      ? "GPS DENIED — dead-reckoned position is unavailable until you record paced distance. Do not use this screen as a current location."\n      : `GPS DENIED — dead reckon ${Math.round(drFix.meters)} m on ${Math.round(drFix.heading)}°${drErrorM != null ? `, estimated ±${drErrorM} m and growing` : ""}.`\n    : deniedNeedHeading\n      ? "Need a compass heading or typed bearing before GPS-denied dead reckon."\n      : !gpsTrusted && gps.fix\n        ? `GPS untrusted — last known ${formatFixAge(gps.fix.recordedAt)}. Display only; not used for off-trail or SOS as live.`\n        : deniedError;''')
replace('''<<<<<<< HEAD\n    ["profile-avy", routeProfileWarnings.avalanche],\n    ["profile-alt", routeProfileWarnings.altitude],\n=======\n    ["route-avy", routeProfileWarnings.avalanche],\n    ["route-altitude", routeProfileWarnings.altitude],\n>>>>>>> 81db907''', '''    ["profile-avy", routeProfileWarnings.avalanche],\n    ["profile-alt", routeProfileWarnings.altitude],''')
replace('''<<<<<<< HEAD\n=======\n  // Ranked by time-to-harm, most urgent first. These used to be a `??` chain rendered\n  // only while on-trail, which hid every one of them the moment you drifted off route —\n  // and ranked the turnaround warning below a hydration nag.\n  const skyWarnings = [\n    deniedError,\n    deniedWarning,\n    checkinOverdue,\n    fallWarning,\n    turnaround,\n    exposureWarning,\n    amsWarn,\n    avyWarn,\n    routeProfileWarnings.avalanche,\n    routeProfileWarnings.altitude,\n    gpsSpoof,\n    sereWarning,\n    ascentWarning,\n    stillWarning,\n    buddyWarn,\n    slopeWarn,\n    commsWarn,\n    daylight?.warning ?? null,\n    hydrateWarning,\n    moonWarning,\n  ].filter((warning): warning is string => Boolean(warning));\n  const skyWarning = skyWarnings[0] ?? null;\n\n  // How old the downloaded route is, so "Offline pack" is not silently ancient.\n  const packAge = useMemo(() => {\n    if (loadState.status !== "ready" || nowMs === 0) return null;\n    const cachedAt = Date.parse(loadState.pack.cachedAt);\n    if (!Number.isFinite(cachedAt)) return null;\n    const days = Math.floor((nowMs - cachedAt) / 86_400_000);\n    if (days >= 1) return `saved ${days} d ago`;\n    const hours = Math.floor((nowMs - cachedAt) / 3_600_000);\n    return hours >= 1 ? `saved ${hours} h ago` : "saved today";\n  }, [loadState, nowMs]);\n>>>>>>> 81db907''', '''  // How old the downloaded route is, so "Offline pack" is not silently ancient.\n  const packAge = useMemo(() => {\n    if (loadState.status !== "ready" || nowMs === 0) return null;\n    const cachedAt = Date.parse(loadState.pack.cachedAt);\n    if (!Number.isFinite(cachedAt)) return null;\n    const days = Math.floor((nowMs - cachedAt) / 86_400_000);\n    if (days >= 1) return `saved ${days} d ago`;\n    const hours = Math.floor((nowMs - cachedAt) / 3_600_000);\n    return hours >= 1 ? `saved ${hours} h ago` : "saved today";\n  }, [loadState, nowMs]);''')
replace('''<<<<<<< HEAD\n                daylightWarning={daylight?.warning ?? null}\n=======\n                daylightWarning={skyWarning}\n>>>>>>> 81db907''', '''                daylightWarning={daylight?.warning ?? null}''')
replace('''<<<<<<< HEAD\n                onToggleGpsDenied={() => (gpsDenied ? exitGpsDenied() : enterGpsDenied())}\n=======\n                onToggleGpsDenied={() => {\n                  toggleDeadReckoning();\n                }}\n>>>>>>> 81db907''', '''                onToggleGpsDenied={toggleDeadReckoning}''')
replace('''<<<<<<< HEAD\n=======\n          </div>\n        )}\n\n        {gps.message && (\n          <div className="pointer-events-none absolute inset-x-3 bottom-32 z-10 rounded-lg border border-amber-500/50 bg-background/95 px-3 py-2 text-xs">\n            {gps.message}\n>>>>>>> 81db907\n          </div>\n        )}''', '''          </div>\n        )}''')
replace('''<<<<<<< HEAD\n              onClick={() => (gpsDenied ? exitGpsDenied() : enterGpsDenied())}\n=======\n              onClick={() => {\n                toggleDeadReckoning();\n              }}\n>>>>>>> 81db907''', '''              onClick={toggleDeadReckoning}''')
assert '<<<<<<<' not in s and '=======' not in s and '>>>>>>>' not in s
path.write_text(s)
