"use client";
import { useState } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { SeverityBadge } from "@/components/safety/severity-badge";
import { DISCLAIMER, bearEncounter, cougarEncounter, foodStorage, mooseEncounter, snakebiteManagement, tickRemoval, wildlifeDistances, type BearBehavior, type BearSpecies } from "@/lib/safety/wildlife";
export function WildlifeSection() {const [species,setSpecies]=useState<BearSpecies>("unknown");const [behavior,setBehavior]=useState<BearBehavior>("aware-calm");const [spray,setSpray]=useState(true);const [cougar,setCougar]=useState<"observed"|"approaching"|"attacking">("observed");const [moose,setMoose]=useState<"observed"|"ears-back"|"charging">("observed");const bear=bearEncounter({species,behavior,sprayAvailable:spray});const cougarResult=cougarEncounter({behavior:cougar});const mooseResult=mooseEncounter({behavior:moose});const snake=snakebiteManagement();return <Card size="sm"><CardHeader><CardTitle>Wildlife encounter</CardTitle></CardHeader><CardContent className="space-y-3 text-xs"><div className="grid grid-cols-2 gap-2"><div><Label htmlFor="wild-species">Bear species</Label><Select value={species} onValueChange={v=>setSpecies(v as BearSpecies)}><SelectTrigger id="wild-species"><SelectValue/></SelectTrigger><SelectContent>{["black","grizzly","unknown"].map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select></div><div><Label htmlFor="wild-behavior">Bear behavior</Label><Select value={behavior} onValueChange={v=>setBehavior(v as BearBehavior)}><SelectTrigger id="wild-behavior"><SelectValue/></SelectTrigger><SelectContent>{["unaware","aware-calm","defensive-bluff","predatory","attacking"].map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select></div></div><div className="flex items-center gap-1"><input id="wild-spray" type="checkbox" checked={spray} onChange={e=>setSpray(e.target.checked)}/><Label htmlFor="wild-spray">Bear spray available</Label></div><div className="rounded border p-2"><SeverityBadge severity={bear.severity}/><ul className="mt-1 list-disc pl-4 text-muted-foreground">{bear.actions.map(line=><li key={line}>{line}</li>)}</ul></div><details><summary className="cursor-pointer font-medium text-foreground">Cougar and moose</summary><div className="mt-2 space-y-2"><Label htmlFor="wild-cougar">Cougar behavior</Label><Select value={cougar} onValueChange={v=>setCougar(v as typeof cougar)}><SelectTrigger id="wild-cougar"><SelectValue/></SelectTrigger><SelectContent>{["observed","approaching","attacking"].map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select><div className="rounded border p-2"><SeverityBadge severity={cougarResult.severity}/><ul className="mt-1 list-disc pl-4 text-muted-foreground">{cougarResult.actions.map(line=><li key={line}>{line}</li>)}</ul></div><Label htmlFor="wild-moose">Moose behavior</Label><Select value={moose} onValueChange={v=>setMoose(v as typeof moose)}><SelectTrigger id="wild-moose"><SelectValue/></SelectTrigger><SelectContent>{["observed","ears-back","charging"].map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select><div className="rounded border p-2"><SeverityBadge severity={mooseResult.severity}/><ul className="mt-1 list-disc pl-4 text-muted-foreground">{mooseResult.actions.map(line=><li key={line}>{line}</li>)}</ul></div></div></details><details><summary className="cursor-pointer font-medium text-foreground">Snakebite and tick removal</summary><div className="mt-2 space-y-2"><p className="font-medium">Snakebite: do</p><ul className="list-disc pl-4 text-muted-foreground">{snake.doList.map(line=><li key={line}>{line}</li>)}</ul><p className="font-medium">Snakebite: do not</p><ul className="list-disc pl-4 text-muted-foreground">{snake.doNotList.map(line=><li key={line}>{line}</li>)}</ul><p className="font-medium">Tick removal</p><ol className="list-decimal pl-4 text-muted-foreground">{tickRemoval().map(line=><li key={line}>{line}</li>)}</ol></div></details><details><summary className="cursor-pointer font-medium text-foreground">Food storage and approach distances</summary><div className="mt-2 space-y-2"><ul className="list-disc pl-4 text-muted-foreground">{foodStorage().map(line=><li key={line}>{line}</li>)}</ul><ul className="space-y-1 text-muted-foreground">{wildlifeDistances().map(entry=><li key={entry.animal}><strong className="text-foreground">{entry.animal}: {entry.minimumMeters} m minimum.</strong> {entry.note}</li>)}</ul></div></details><p className="text-[10px] text-muted-foreground">{DISCLAIMER}</p></CardContent></Card>;}
